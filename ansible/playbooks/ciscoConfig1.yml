---
- hosts: CSR1
  gather_facts: fasle
  connection: local
  tasks:
  - name: Show run
    ios_commands:
      commands:
      - en
      - conf t
      - int g2
      - ip addr 10.42.42.42 255.255.255.0
      - desc This was set by Ansbile
      - no shut
      - do wr mem
      